# Home Assistant API-Only Addon for Docker Wyze Bridge
# Uses the pre-built API-only image with proper tagging
FROM waelewida/wyze-bridge:api

LABEL maintainer="Wael Ewida <waelsamy@mail.com>"
LABEL description="Home Assistant addon using API-only Docker image"

# API image already has proper configuration, just expose the port
EXPOSE 5000
